[phases.setup]
nixPkgs = ["nodejs_20", "python3", "gnumake", "gcc", "chromium", "nss", "freetype", "harfbuzz", "ca-certificates", "ttf-opensans"]

[phases.install]
cmds = ["npm ci --omit=dev"]

[variables]
PUPPETEER_SKIP_CHROMIUM_DOWNLOAD = "1"

[start]
cmd = "node server.js"
